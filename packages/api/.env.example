# Example environment variables for API service

# --- Dummy Data (safe for sharing) ---
PORT=3000
DATABASE_URL=postgresql://chatapp:chatapp@localhost:5432/chatapp
MINIO_ENDPOINT=http://localhost:9000
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_BUCKET=chatapp-media

# --- Actual Data (replace with your real secrets in .env, do NOT commit real values) ---
# PORT=3000
# DATABASE_URL=postgresql://<username>:<password>@<host>:<port>/<database>
# MINIO_ENDPOINT=http://<minio-host>:9000
# MINIO_ROOT_USER=<your-minio-user>
# MINIO_ROOT_PASSWORD=<your-minio-password>
# MINIO_BUCKET=<your-bucket>
